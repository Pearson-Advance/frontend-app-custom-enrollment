/* eslint-disable import/prefer-default-export */
// To avoid default export warning(exporting in a different way won't render the dashboard content.)
import React from 'react';

export const Dashboard = () => (
  <main>
    <section>
      <h1>Initial Template</h1>
    </section>
  </main>
);
